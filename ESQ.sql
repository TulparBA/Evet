select @@CONNECTIONS as 'Number of Login Attemps'

SELECT * FROM Ogrenci
if(@@ERROR<>0)
print 'Error Found'
else
print'Error Not Found'

select @@IDLE as 'Idle Milliseconds Time'
select @@LANGUAGE as 'Language'
select min(Copies) from [BooksLibrary].[dbo].[TotalBooks]
select sum(Cinsiyet)
